# Linux
Speech-to-text
```sh
sudo apt-get install libportaudio2 libcanberra-gtk-module libcanberra-gtk3-module
sudo snap install buzz

```

```sh
grep -rl '_juni_' ~/Documents/deutsch/deutsch/_/Sätze/SätzeArch | while 
read file; do     mv "$file" ~/Documents/deutsch/deutsch/_/Sätze; done
```